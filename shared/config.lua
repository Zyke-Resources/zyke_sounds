Config = Config or {}

Config.Settings = {
    debug = false,
    volumeUpdateInterval = 100,
    commandName = "sounds",
    kvpKey = "zyke_sounds:volumeMultiplier", -- Unique key to store the volume multipliers, probably don't touch
}